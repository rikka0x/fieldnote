# minimal repo layout

Question: what is enough structure for a note repo?

Current answer:

- `notes/` for dated notes
- `snippets/` for tiny code
- `experiments/` for throwaway tests

Anything more can wait.
